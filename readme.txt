To run this program you need to install Node.js

In the project folder, run npm start.
This will install the dependencies and start the http server on port 8000.

Open your favourite web browser and go to localhost:8000